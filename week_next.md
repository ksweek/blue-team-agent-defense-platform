# GuardianAgent 项目介绍

## 一、项目大概是做什么的

GuardianAgent 是一套面向 Function-Calling Agent 的多维防御平台。

简单来说，它不是再做一个普通的 AI 聊天页面，而是给 AI Agent 加上一层安全防护和运行监控。当 AI Agent 开始调用工具、插件、MCP Server、本地 Runtime，甚至访问文件、执行操作时，系统可以提前判断这些动作是否安全，必要时进行拦截，并把整个过程记录下来，方便后续查看和复盘。

可以把它理解成：放在 AI Agent 前面的一道“安全检查站”。

它主要关注的问题是：AI Agent 在生产环境中怎样安全运行、怎样被统一管理、怎样发现风险、怎样留下证据。

## 二、具体解决哪些问题

在实际使用 AI Agent 时，如果只是让模型直接去调用工具，会出现很多安全和管理问题，例如：

- 不同 AI Agent 接入方式不同，后期会越来越乱，缺少统一入口。
- Agent 可以调用工具、插件、MCP，可能会访问敏感文件、执行危险操作或越权调用能力。
- Prompt 注入、越狱攻击、工具滥用、MCP 权限扩大等风险不容易提前发现。
- 出现安全问题后，只能临时翻日志，缺少清晰的安全事件记录和报告。
- 不同业务的 AI Agent 风险不同，需要分别设置规则，而不是所有 Agent 都用同一套配置。
- 生产环境中需要知道 Agent 是否在线、是否触发风险、风险有没有被拦截、后续能不能追溯。

GuardianAgent 主要就是为了解决这些问题：

- 提供统一的 AI / Agent 安全接入入口。
- 在工具调用、MCP 调用、Runtime 执行前先做安全判断。
- 对高风险行为进行拦截、授权或记录。
- 按单个 AI 目标配置防护规则和治理策略。
- 记录安全事件，生成报告，形成可追溯的证据链。
- 支持 OpenClaw、Runtime、MCP 等更接近真实 Agent 场景的接入方式。

一句话概括：它让 AI Agent 不只是“能运行”，而是“能被安全地运行、监控和管理”。

## 三、当前完成情况

目前项目已经不是单纯的概念设计，而是一个可以本地运行、可以演示、可以继续扩展的 Beta 原型。

已经完成的主要内容包括：

- 完成了前后端基础平台，具备登录、仪表盘、配置管理、事件查看、报告查看等页面。
- 完成了 AI 目标管理，可以对不同 Agent 或模型接入目标进行单独管理。
- 完成了统一安全网关，用来接收和管理 AI / Agent 请求。
- 完成了防御规则与策略配置，可以对 Prompt 注入、工具滥用、敏感路径访问等风险进行检测。
- 完成了攻击测试功能，可以选择攻击样本，对目标 Agent 做安全验证。
- 完成了安全事件记录，系统可以把风险命中、拦截结果和任务结果保存下来。
- 完成了报告导出能力，支持把安全测试和事件结果整理成报告。
- 完成了 MCP 相关防护，包括 Server、Capability、Scope、审批和一次性执行票据等控制。
- 完成了 OpenClaw / Runtime 接入链路，可以让外部运行时接入平台并接受安全审查。
- 完成了 Redis 缓存、启动脚本、Docker 配置和运行环境打包，方便演示和交付。

从当前阶段来看，项目已经具备比较完整的核心链路：接入目标、发起测试、执行任务、判断风险、记录事件、生成报告。

如果按演示、答辩、联调和继续开发来衡量，当前完成度比较高；如果按大规模生产环境直接上线来衡量，还需要继续加强稳定性、部署能力和运维能力。

## 四、待改善情况

虽然项目已经能跑通核心流程，但仍然有一些地方可以继续完善：

- 生产环境稳定性还可以继续加强，例如更完善的高并发测试和服务监控。
- 后台任务调度目前还可以继续优化，后续可以接入更成熟的队列系统。
- 数据库在生产环境中更适合使用 PostgreSQL，避免本地 SQLite 在高并发写入下出现限制。
- 前端页面已经可用，但部分页面还可以继续优化布局、交互和展示效果。
- 防御规则还可以继续扩展，减少误报和漏报，覆盖更多真实攻击场景。
- 报告能力还可以继续增强，例如增加 PDF 报告、趋势对比、风险评分等。
- 可以继续接入更多真实 Agent Runtime、模型服务和业务系统，让平台更贴近实际生产环境。
- 自动化测试和压力测试还可以继续补充，让项目长期迭代时更稳定。

简单来说，当前项目的核心功能已经打通，后续重点是让它更稳定、更易用、更适合真实生产环境。

## 五、参加本项目可以学到什么

参与这个项目可以学到的不只是单个页面或接口开发，而是一套比较完整的 AI 安全平台开发经验。

可以学习到：

- AI Agent 和 Function Calling 的基本工作方式。
- Prompt 注入、越权调用、工具滥用、MCP 风险等 AI 安全问题。
- 如何设计一个统一的 AI 安全网关。
- 如何在 Agent 调用工具之前做授权、拦截和审计。
- 如何设计安全事件、任务、报告这些完整业务流程。
- FastAPI、SQLAlchemy、JWT、后台 Worker 等后端开发内容。
- Vue 3、TypeScript、Vite 等前端开发内容。
- Redis 缓存、Docker 部署、启动脚本、运行环境打包等工程化内容。
- 如何把一个项目从“能跑”推进到“能演示、能交付、能继续扩展”。
- 如何写项目文档、整理报告、做演示汇报和技术答辩。

对于想学习 AI 安全、Agent 应用开发、前后端工程、平台型系统设计的同学来说，这个项目都比较适合。

## 六、总结

GuardianAgent 的核心价值是：让 AI Agent 在调用工具、插件、MCP 和 Runtime 时，不再处于无人管理的状态，而是能够被统一接入、统一防护、统一监控和统一复盘。

它解决的是 AI Agent 进入实际业务之后最容易遇到的问题：能不能安全地接入，风险能不能提前拦住，出了问题能不能查清楚。

当前项目已经完成主要功能闭环，适合作为 AI Agent 安全防护平台的演示原型和继续迭代的基础。后续如果继续完善部署、稳定性、测试覆盖和真实业务接入能力，就可以进一步向更成熟的生产级平台发展。
